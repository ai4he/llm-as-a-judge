# --- secrets (never commit) ---
secrets.sh
.env
*.key

# --- python ---
__pycache__/
*.pyc
.ipynb_checkpoints/

# --- LaTeX build artifacts (paper/main.pdf IS tracked as a snapshot) ---
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/compile.log

# --- misc ---
.DS_Store
texinstall.log

# Study 3 raw datasets (licenses/ethics: not redistributed)
study3/data_raw/
study3/outputs/*.raw.jsonl

# agent/session files (may contain secrets) - never commit
.claude/
