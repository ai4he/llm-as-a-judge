# Copy to secrets.sh (git-ignored) and fill in.  Usage:  source secrets.sh
export OPENALEX_API_KEY="your-openalex-api-key"        # Studies 1-2 search (optional; polite-pool via mailto)
export RCD_LLM_API_KEY="your-clemson-rcd-llm-api-key"  # Study 3 -- never commit (docs warn against this)
export RCD_LLM_BASE_URL="https://llm.rcd.clemson.edu/v1"
